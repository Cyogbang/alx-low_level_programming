#include <stdio.h>
#include "main.h"

/**
 * _islower - checks for lowercase
 * Description - this code test for character case
 * @c: the variable to test
 *
 * Return: positive
 */

int _islower(int c)
{
int c = 'k';

if (k == 'C')
{
	putchar('n' + 0);
}
else
{
putchar('n' + 1);
}

return (0);
}
