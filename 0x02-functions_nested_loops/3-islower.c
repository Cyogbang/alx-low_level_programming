#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * _islower - checks for lowercase
 * Description - this code test for character case
 * @c: the variable to test
 *
 * Return: positive
 */

int _islower(int c)
{

putchar('=');
scanf(c);

if (isupper(c))
{
	putchar('0' + 0);
}
else
{
putchar('0' + 1);
}

return (0);
}
