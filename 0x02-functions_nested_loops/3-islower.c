#include <stdio.h>
#include "main.h"

/**
 * _islower - checks for lowercase
 * Description - this code test for character case
 * @c: the variable to test
 *
 * Return: positive
 */

int _islower(int c)
{

if (c == 'C')
{
	putchar(r + '0');
}
else
{
putchar(r + '1');
}

return (0);
}
