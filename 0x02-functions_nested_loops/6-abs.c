#include "main.h"
/**
 * _abs - computes the ansolute value of an integer
 * @n: the integer to check
 *
 * Return: the absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
